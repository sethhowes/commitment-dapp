import { ethers } from "hardhat";
import { expect } from "chai";

describe("Commit contract", () => {
  it("Should deploy the contract successfully", async () => {});
  it("Should lock up funds when a user commits to a run", async () => {});
  it("Should whether the user has currently committed to a run", async () => {});
  it("Should return 20% of locked balance when completing a run", async () => {});
});
