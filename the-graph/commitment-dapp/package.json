{
  "name": "commitment-dapp",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ commitment-dapp",
    "create-local": "graph create --node http://localhost:8020/ commitment-dapp",
    "remove-local": "graph remove --node http://localhost:8020/ commitment-dapp",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 commitment-dapp",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.68.2",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
